###########################################
Image Pull Mechanism
###########################################

Get a token -> we get options to choose -> we choose to get digest of manifest -> \
it helps to fetch digest of of tar.gz file -> Download the blog into chunks to tar file \
extract tar file inside jail_directory


-----------------------------------------------------------------------------------

